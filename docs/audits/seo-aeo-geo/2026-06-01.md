# SEO / AEO / GEO Audit — 2026-06-01

_Automated monthly audit (first run). Branch: `chore/seo-audit/2026-06`. **Nothing has been pushed** — these are local commits for you to review and push when ready._

---

## 📘 In Plain English (start here)

- **Your SEO foundations are healthy.** All five of the project's built-in quality checks passed: no broken internal links, no "thin"/low-quality pages, no missing pages in the sitemap, and no leftover placeholder text.
- **I made two safe improvements** (already committed locally on the branch above):
  1. The homepage now tells Google and AI engines (ChatGPT, Perplexity, Google AI Overviews) **who SwingIQ is as an organization and website** — important "who are you" signals it was missing before.
  2. I expanded `llms.txt` (the plain-text guide AI assistants read) so it points them to **all your best answer pages** — tools, guides, challenges, benchmarks, and blog — not just a handful.
- **One thing needs you (or a designer):** the site has **no social-share image and no app icon/favicon**. Every time someone shares a SwingIQ link (text message, Facebook, Slack, etc.) there's a blank preview, and there's no logo in search "knowledge" data. This needs real artwork — I won't invent a logo. See **Recommendation P1**. I can wire up auto-generated branded images in code if you want.
- **I deliberately skipped ~16 marketing pages** (About, Blog, FAQ, Features, Pricing, etc.) because they're **mid-edit in your theme work** right now. Auditing them now would tangle my changes into your unfinished work. They'll be covered next month once that work is committed.
- **Heads-up (not SEO):** your in-progress `video/SportVideoAnalyzerContent.tsx` showed a code parse error in the dev server during this run. `tsc` now reports it clean, so it looks already-resolved — just flagging it.

**To review:** look at branch `chore/seo-audit/2026-06` (2 commits + this report). Push it yourself when you're happy.

---

## What was checked

| Area | Check | Result |
|---|---|---|
| SEO | Built-in growth audit (placeholders, thin content, links, sitemap, content registry) | ✅ All 5 passed |
| SEO | TypeScript type-check (web) | ✅ Passed |
| SEO | ESLint (web) | ✅ 0 errors (126 pre-existing warnings, unrelated) |
| SEO | Referenced static assets exist (OG image, icons) | ❌ Missing — see P1 |
| GEO | Site-wide Organization + WebSite structured data | ⚠️ Was missing → **Fixed** |
| GEO | `llms.txt` coverage of answer pages | ⚠️ Sparse → **Improved** |
| AEO | FAQ / HowTo structured data on key pages | ✅ Present where expected (homepage, sport pages) |
| SEO | Canonical URLs / Open Graph via shared helper | ⚠️ Many pages hand-roll instead of using `buildMetadata` → see P2 |

## Findings & status

| # | Area | Finding | Status |
|---|---|---|---|
| 1 | GEO | Homepage emitted only `WebApplication` + `FAQPage`; no `Organization` or `WebSite` entity nodes anywhere on the site. | **Fixed** (homepage now uses the canonical schema builders → Organization + WebSite + SoftwareApplication + FAQPage). |
| 2 | GEO/AEO | `public/llms.txt` listed ~11 pages; dozens of high-value answer pages (tools, guides, challenges, benchmarks, blog) were invisible to AI crawlers. | **Fixed** (expanded to categorized listing of all public answer pages). |
| 3 | SEO/Social | `og-default.png`, `icon-192.png`, `icon-512.png` are referenced (metadata, JSON-LD logo, manifest) but **do not exist** in `apps/web/public/`. | **Needs owner** — P1 (requires brand artwork). |
| 4 | SEO | Sport pages & several others hand-roll `metadata` + JSON-LD instead of using `buildMetadata()` / the schema builders. Not broken, but inconsistent (e.g. some lack explicit canonical). | **Needs review** — P2. |
| 5 | All | ~16 public marketing pages are uncommitted/mid-edit (theme tokenization) — skipped to avoid entangling. | **Deferred** to next run. |
| 6 | Accuracy | `llms.txt` states SwingIQ "does not provide ... computer vision pixel-level analysis." Verify this still matches the current AI-vision video analyzer. | **Needs review** — P3 (claim left unchanged on purpose). |

## Changes made (committed locally — NOT pushed)

Branch: `chore/seo-audit/2026-06`

1. `apps/web/src/app/page.tsx` — replaced inline JSON-LD with a builder-based graph adding **Organization** and **WebSite** entity nodes (reuses `lib/seo/jsonLd.ts`). Verified: homepage returns HTTP 200 and renders `@graph` = `[Organization, WebSite, SoftwareApplication, FAQPage]`.
2. `apps/web/public/llms.txt` — expanded page directory into categories (sport analysis, answer guides, free tools, challenges, benchmarks, blog, product/trust) so generative engines can discover answer-rich pages.

No new content pages were drafted this run (focused on safe technical/GEO wins given the in-progress working tree). Content-gap drafting is queued for a future run — see P4.

## New draft pages created

None this run.

## Prioritized recommendations (need a human)

**P1 — Add social-share image + app icons (highest impact).**
Every shared SwingIQ link currently has no preview image, there's no favicon, no installable PWA icon, and the Organization logo in structured data 404s. Two paths:
- (a) Add real artwork: drop `og-default.png` (1200×630), `icon-192.png`, `icon-512.png` into `apps/web/public/`; or
- (b) Let me implement Next.js dynamic image generation (`app/opengraph-image.tsx` + `app/icon.tsx`) using your existing brand green (`#1a3a2a`) and the "SQ" wordmark — pure code, no design files needed. Say the word.

**P2 — Standardize metadata/schema on the shared helpers.**
Migrate the sport-analysis pages (and others that hand-roll) to `buildMetadata()` + the `jsonLd.ts` builders so every page gets a consistent canonical, Twitter card, and Organization linkage. Low risk; do it when those files aren't mid-edit.

**P3 — Verify the `llms.txt` capability claim** about computer-vision analysis still matches today's AI-vision video analyzer, and adjust if needed.

**P4 — Content gaps to consider drafting next run.**
Once the marketing/theme work lands, the next run can draft (as `publishStatus:'draft'`, for your review) new keyword landing pages where competitors rank and SwingIQ has no page yet.

**P5 — Re-audit the ~16 in-progress marketing pages** next month after the theme-tokenization work is committed.

---

_Generated by the `seo-aeo-geo-monthly-audit` scheduled task. Local commits only — review and push at your discretion._
