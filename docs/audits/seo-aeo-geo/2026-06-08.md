# SEO/AEO/GEO — Technical sitemap, canonical & schema upgrade (2026-06-08)

One-time implementation pass (not the monthly cron audit). Shipped to
`origin/master`. Full engineer + owner docs live in [`docs/seo/`](../../seo/README.md).

## Shipped (code)
- **Curated URL registry** — `apps/web/src/lib/seo/site-sections.ts`: single
  source of truth for curated public URLs, grouped by section. Consumed by both
  the XML sitemap and the new HTML sitemap so they cannot drift. Split-ready.
- **HTML sitemap** at **`/sitemap`** — `apps/web/src/app/(marketing)/sitemap/page.tsx`:
  sectioned, descriptive labels, self-canonical, BreadcrumbList schema. Linked
  from the footer. Coexists with `/sitemap.xml`.
- **XML sitemap refactor** — `app/sitemap.ts` now reads the registry; dynamic
  groups (guides/blog/challenges/library/i18n) unchanged.
- **robots.txt** — fixed stale `/report/sample` → `/sample-report`; directory-level
  guide allows; added pickleball/padel hubs + `/sitemap`.
- **Gates updated** — `scripts/check-sitemap.mjs` + `check-sitemap-coverage.mjs`
  now scan the registry too.

## Verified
- `check-sitemap.mjs` ✅ 56 paths resolve · `check-sitemap-coverage.mjs` ✅ 95 pages covered (both on the origin base)
- `tsc --noEmit` ✅ · `next build` ✅ exit 0 · SEO jest suite ✅ 30 passed · eslint ✅ clean

## Already in good shape (no rebuild)
Self-canonical via `buildMetadata()` (query params collapse to clean path);
full JSON-LD builder set (Org/WebSite/SoftwareApplication/Article/FAQ/HowTo/
Breadcrumb/Service) with XSS-safe serializer; strong internal linking (footer,
`RelatedGuides` silos, breadcrumbs); hreflang i18n.

## Open items needing OWNER decision (tracked as findings)
- **F-30…F-35** — six blog↔guide cannibalization pairs (near-identical/identical
  title + intent). Per pair: differentiate + cross-link, canonicalize, noindex,
  or merge. Detail + recommendations: [`docs/seo/duplicate-cannibalization-report.md`](../../seo/duplicate-cannibalization-report.md).
- **F-36** (extends F-21) — submit the refreshed `sitemap.xml` + top-20 priority
  URLs to Google Search Console. Runbook: [`docs/seo/gsc-submission-checklist.md`](../../seo/gsc-submission-checklist.md),
  [`docs/seo/priority-submit-urls.md`](../../seo/priority-submit-urls.md) (provisional, pending approval).

Tier-2/3 submission rules: [`docs/seo/tiered-indexation-strategy.md`](../../seo/tiered-indexation-strategy.md).
Weekly GSC monitoring: [`docs/seo/weekly-gsc-monitoring.md`](../../seo/weekly-gsc-monitoring.md).
