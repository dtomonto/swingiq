// ============================================================
// Link Intelligence Agent — shared constants
// ============================================================

/** Stamped on records the agent creates (provenance in GrowthOS tables). */
export const AGENT_OWNER = 'Link Intelligence Agent';

/** Marketing site domain the agent reasons about. */
export const TARGET_DOMAIN = 'swingvantage.com';
