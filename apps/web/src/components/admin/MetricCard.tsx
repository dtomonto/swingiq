// MetricCard — canonical import path for the KPI tile. The implementation (and
// the backward-compatible `MetricStat` alias) lives in ./MetricStat.
export { MetricCard, MetricStat } from './MetricStat';
export type { MetricCardProps, MetricStatProps } from './MetricStat';
