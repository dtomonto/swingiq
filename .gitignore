# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
.next
dist
out
build
.expo

# TypeScript incremental build cache (regenerated on every tsc/type-check run)
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# Turbo
.turbo

# Claude Code internal worktrees (transient, machine-local)
.claude/worktrees/
# Claude Code per-machine permission allowlist — stays local, not shared
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode
.idea
*.swp
*.swo

# Test coverage
coverage
.nyc_output

# Expo
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
android/
ios/

# Supabase
supabase/.temp

# Secret / credential files — never commit these
*.pem
*.crt
*.pfx
service-account*.json
secrets.json
credentials.json
*-service-account.json
firebase-adminsdk*.json

# Test output
playwright-report
test-results
**/e2e/__screenshots__/

# Vercel
.vercel

# Security report outputs (generated by CI and local scans — not committed)
security-reports/*.json
security-reports/*.txt
